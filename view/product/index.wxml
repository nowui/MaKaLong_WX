<view class="container">
  <view class="header"></view>
  <view class="product-info margin-top">
    <view class="product-info-name">{{product_name}}</view>
    <view class="product-info-price">￥{{product_price}}</view>
  </view>
  <view class="margin-top" wx:for="{{product_image_file_list}}" wx:for-item="video" wx:key="{{index}}">
    <video style="width: {{window_width}}px; height: {{window_width / 100 * 56}}px;" src="{{video.file_path}}"
    poster="{{video.file_image}}" controls></video>
    <view class="video-name">{{video.file_name}}</view>
  </view>
  <view style="height: 10px;"></view>
</view>